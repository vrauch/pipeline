<%= form_for NoteComment.new, url: note_comments_path(note), remote: true do |f| %>
  <%= f.text_area :body, class:'form-control request_field', placeholder: 'Type comment here' %>
  <%= f.submit 'Comment', class:'btn btn_add', disabled: :disabled %>
<% end %>